---
import clsx from 'clsx';

interface Props {
  extraClasses?: string;
  children: any;
}

const { extraClasses } = Astro.props;
---

<div class={clsx('mx-auto max-w-3xl px-4', extraClasses)}>
  <slot />
</div>
